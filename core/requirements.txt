fusepy>=1.0.1
tabulate>=0.9.0  # For nice table formatting
# GUI
PySide6
markdown-it-py
mdit-py-plugins
# Optional ACL backend and schema validation
casbin>=1.43.0
pydantic>=2.12.5
# Note: fusepy must be installed system-wide or via apt
