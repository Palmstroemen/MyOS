# Legacy-Code-Kandidaten (verwaister Code)

Jeder Eintrag wurde zweimal geprüft: Es gibt **keinen Aufruf** außer der Definition bzw. nur Aufrufe innerhalb ebenfalls ungenutzter Funktionen.

---

## 1. main.qml

| Zeile(n) | Name | Art | Prüfung |
|----------|------|-----|--------|
| 756–759 | **`_isMyOSFolderTrace(pathOrName)`** | Funktion | Nur 1 Vorkommen im Repo (Definition). Nirgends aufgerufen. |
| 2134–2140 | **`shortenPathForDisplay(path)`** | Funktion | Nur 1 Vorkommen (Definition). Nirgends aufgerufen. |
| 2142–2144 | **`lastPathSegment(path)`** | Funktion | Nur 1 Vorkommen (Definition). Nirgends aufgerufen. |
| 2147–2154 | **`parentPaths()`** | Funktion | Wird nur von `visibleParentPaths()` aufgerufen; beide werden von außen nicht genutzt (FolderBrowser hat eigene Implementierung). |
| 2156–2160 | **`visibleParentPaths()`** | Funktion | Nur Definition + interner Aufruf von `parentPaths()`. Kein Aufruf aus main oder von anderen Komponenten. |
| 168 | **`maxVerticalParents`** | Property | Wird nur in `visibleParentPaths()` verwendet. Da diese Funktion verwaist ist, ist die Property effektiv Legacy. |

---

## 2. FolderBrowser.qml

| Zeile(n) | Name | Art | Prüfung |
|----------|------|-----|--------|
| 117 | **`signal toggleTheme()`** | Signal | Nur 1 Vorkommen (Deklaration). Wird nirgends emittiert (`toggleTheme()` kommt nicht vor). In main existiert kein `onToggleTheme`; `showThemeToggle` ist für beide Browser `false`, ein Theme-Button würde also nie angezeigt/geklickt. |

---

## Kurzfassung

- **main.qml:** 1 Property und 5 Funktionen (davon 2 nur untereinander genutzt), insgesamt **ca. 25 Zeilen** Kandidat.
- **FolderBrowser.qml:** 1 Signal (1 Zeile) Kandidat.

**Hinweis:** Vor dem Entfernen empfiehlt sich ein kurzer Abgleich mit der Versionsgeschichte (z. B. ob `_isMyOSFolderTrace` oder die Pfad-Helfer für geplante Features oder Tests gedacht waren).
